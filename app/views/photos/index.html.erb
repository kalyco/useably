<div class="right-col">
  <div class="photos-wrapper">
    <div id='container'>
    <% @photos.each do |photo| %>
      <div class='item'><%= link_to image_tag(photo.image_file), photo_path(photo) %>
      </div>
      <%= photo.likes %>
      <div class="roight"><%= photo.time_added %></div>
    <% end %>
      </div>
    </div>
  </div>
